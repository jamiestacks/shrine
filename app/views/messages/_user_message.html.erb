 <div id="message-<%= message.id %>">
    <small>
      <strong class = "author"><%= message.user.first_name %></strong>
      <i class = "timestamp"><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
    </small>
      <p class = "message-content"><%= message.content %></p>
</div>
